let browsers = ['Chrome', 'FireFos', 'Edge', 'Safari', 'Opera'];

for ( let index in browsers) {
    console.log(browsers[index]);
}

console.log("----------");

for ( let browser of browsers) {
    console.log(browser);
}